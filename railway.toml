[build]
  builder = "docker"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "./friday"
  healthcheckPath = "/trading/status"
  healthcheckTimeout = 5000
  restartPolicy = "on-failure"
  restartPolicyMaxRetries = 5